function _validateNewListing(ListingParameters memory _params, TokenType _tokenType)
├── when quantity is zero
│   └── it should revert
└── when the quanitty is not zero
    ├── when the quantity not one and token type is ERC721
    │   └── it should revert
    └── when the quantity is one or token type is ERC1155
        ├── when the token owner owns less than the quantity to list or marketplace is not approved to transfer tokens
        │   └── it should revert
        └── when the token owner owns gte the quantity to list and marketplace is approved to transfer tokens
            └── it should return