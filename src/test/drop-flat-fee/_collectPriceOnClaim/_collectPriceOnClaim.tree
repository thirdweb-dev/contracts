function _collectPriceOnClaim(
    address _primarySaleRecipient,
    uint256 _quantityToClaim,
    address _currency,
    uint256 _pricePerToken
)
├── when _pricePerToken is equal to zero
│   ├── when msg.value does not equal to zero
│   │   └── it should revert ✅
│   └── when msg.value is equal to zero
│       └── it should return ✅
└── when _pricePerToken is not equal to zero
    ├── when totalPrice < platformFees
    │   └── it should revert ✅
    └── when totalPrice >= platformFees
        ├── when _primarySaleRecipient is equal to address(0)
            └── it should transfer to stored primarySaleRecipient (total - platformFees) and platformFeeRecipient (platformFees) ✅
        └── when _primarySaleRecipient is not equal to address(0)
            └── it should transfer to _primarySaleRecipient (total - platformFees) and platformFeeRecipient (platformFees) ✅